export enum ModeType {
    LIGHT = "light",
    DARK = "dark",
};

export enum Health {
    NONE = 0,
    LOW = 1,
    MEDIUM = 2,
    HIGH = 3
};

export enum BillCategory {
    ENTERTAINMENT = "entertainment",
};